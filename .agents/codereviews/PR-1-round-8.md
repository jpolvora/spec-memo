# PR-1 Round 8

Fail-closed bootstrap budget; sanitize all CLI --json prints; extend vault path key set.
Learning: Bootstrap fail-closed when skeleton exceeds budget
Learning: N/A for CLI path leak (already covered by Sanitize every caller-facing seam)
