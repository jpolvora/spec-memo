# PR-1 Round 7

Stage only mutation-scoped vault paths; never `git add .`.
Score 5 drift-catch resolved with no code (best-effort git; inner show-fail already counts as drift).
Learning: Vault git auto-commit must not git add .
