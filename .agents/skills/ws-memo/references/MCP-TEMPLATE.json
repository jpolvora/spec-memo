{
  "_comment": "Canonical stdio MCP snippet (server key must be spec-memo to match specMemo.mcpServerName). Primary npx: npm package. Fallback: github:jpolvora/spec-memo. For HTTP/SSE use memo serve --sse → http://127.0.0.1:3000/sse; status UI http://127.0.0.1:3001/ (not MCP).",
  "mcpServers": {
    "spec-memo": {
      "command": "memo",
      "args": ["serve"]
    },
    "spec-memo-npx": {
      "command": "npx",
      "args": ["-y", "spec-memo", "serve"]
    },
    "spec-memo-npx-git": {
      "command": "npx",
      "args": ["-y", "github:jpolvora/spec-memo", "serve"],
      "_comment": "Fallback when npm registry package is unavailable; prefer spec-memo-npx."
    }
  }
}
