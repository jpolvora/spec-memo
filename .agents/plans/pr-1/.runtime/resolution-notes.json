{
  "default": "Fixed in this round. defectClass: see thread. sourcesConsulted: code, memory (consult-skipped then compiled), context, patterns (consult-skipped). proactiveFixed: same-class siblings in this PR. proactiveSkipped: none.",
  "notes": {
    "spec-drift": "Fixed spec drift to compare file content at verifiedAtSha (git show) vs the working tree, not last-touch commit SHA. Unrelated later commits no longer false-positive. sourcesConsulted: code, memory, context. proactiveFixed: bootstrap.ts checkSpecDrift (threads 1 and 14). proactiveSkipped: none.",
    "truncation": "Fixed bootstrap truncation to also trim activeSlice then drift until byteLength <= budgetBytes (fail-closed). sourcesConsulted: code, context. proactiveFixed: bootstrap.ts compileBootstrapBrief (threads 2 and 13). proactiveSkipped: none.",
    "path-leak": "Caller-facing MCP/CLI payloads now strip path/filepath/targetPath via sanitizeToolOutput. Internal store still keeps vault paths. sourcesConsulted: code, context. proactiveFixed: tools.ts executeTool plus siblings in indexer/store/schema/promote/types. proactiveSkipped: none (pathPatterns and destination kept).",
    "secrets-output": "Read-path secret redaction now runs on bootstrap/search/get/upsert/append/gc/promote tool output. sourcesConsulted: code, context. proactiveFixed: safety.redactSecretsInPayload used from executeTool (threads 8 and 9). proactiveSkipped: none.",
    "empty-patterns": "Trap dedup now requires identical pathPatterns; empty arrays are not a wildcard. sourcesConsulted: code, context. proactiveFixed: store.ts (thread 10). proactiveSkipped: none.",
    "vault-branch": "syncVault uses config.vaultGit.branch or the current HEAD branch, not hardcoded main. sourcesConsulted: code, context. proactiveFixed: vault.ts (thread 11). proactiveSkipped: none.",
    "log-ids": "Append log IDs now include pid plus random hex so parallel processes cannot collide. sourcesConsulted: code, context. proactiveFixed: store.appendEvent (threads 12 and 19). proactiveSkipped: none.",
    "dedup-lock": "Trap upsert and vault git commits take a re-entrant exclusive .memo.lock. sourcesConsulted: code, context. proactiveFixed: vault.withVaultLock used from store.upsertRecord and commitVaultChange (threads 20 and 23). proactiveSkipped: none.",
    "isGit-guard": "Product-tree write guard already applies when isGit is false (commit f402588). No further code change. sourcesConsulted: code, context. proactiveFixed: none this round. proactiveSkipped: none.",
    "gc-fts": "runGc now awaits rebuildIndex after live GC and sets rebuiltFts from that work. sourcesConsulted: code, context. proactiveFixed: curator.ts (thread 22). proactiveSkipped: none.",
    "vaultRoot-schema": "Advertised MCP inputSchema no longer includes vaultRoot; zod still accepts it for tests/CLI. Search description no longer says 'in vault'. sourcesConsulted: code, context. proactiveFixed: all eight tool schemas (threads 24-32). proactiveSkipped: none.",
    "hook-backup": "installPreCommitHook copies an existing unrelated hook to pre-commit.spec-memo.bak before overwrite. sourcesConsulted: code, context. proactiveFixed: hook.ts (thread 33). proactiveSkipped: none."
  }
}
